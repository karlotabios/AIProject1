import java.util.*;
import java.io.File;

public class GreedyStrategy extends Strategy {

	public GreedyStrategy( int maxNumDirections, int initialState, ArrayList<Integer> initialFringe )
	{
		super( maxNumDirections, initialState, initialFringe );
	}

	public int traverse( ArrayList<Integer> childNodes, ArrayList<Integer> childNodeWeights )
	{
		int minimumWeight = childNodes
		for (int i = 0; i < maxNumDirections; i++) {

		}

		return fringe.get( ++currentChildIndex );
	}
}